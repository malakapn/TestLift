# TestLift
Tester uploads the Excel/word document,  AI reads, automates in py or java files then translates back in NLP for the testers to approve the document. Once approved, it makes the file ready for CI/CD. Once the the build is deployed in QA environment, it gets tested.
